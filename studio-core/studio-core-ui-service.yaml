apiVersion: v1
kind: Service
metadata:
  name: kub-studio-core-ui-service
spec:
  type: NodePort
  selector:
    app: studio-ui
    tier: frontend
  ports:
      # By default and for convenience, the `targetPort` is set to the same value as the `port` field.
    - port: 8081
      targetPort: 8081